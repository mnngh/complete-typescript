const superHeros: string[] = ['thor', 'superman', 'ironman'];

superHeros.push('spiderman');

console.log(superHeros);

// const heroPower: number[] = []

const heroPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

export {};
